interface:
  display_name: "Maincraft"
  short_description: "Repo-specific patterns and workflows for maincraft"
  default_prompt: "Use the maincraft repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true